import { StudentDetailsHelper } from './jsonModifier.helper';

describe('StudentDetailHelper', () => {
  it('should create an instance', () => {
    expect(new StudentDetailsHelper()).toBeTruthy();
  });
});
