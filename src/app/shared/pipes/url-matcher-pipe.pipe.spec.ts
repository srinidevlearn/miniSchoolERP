import { UrlMatcherPipe } from './url-matcher-pipe.pipe';

describe('UrlMatcherPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UrlMatcherPipe();
    expect(pipe).toBeTruthy();
  });
});
